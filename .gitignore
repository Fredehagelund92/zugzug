# dependencies
node_modules/

# build output
dist/

# secrets / local env (DATABASE_URL, MOTHERDUCK_TOKEN live here)
.env
.env.*
!.env.example
!.env.prod.example

# local DuckDB engine files
*.duckdb
*.duckdb.wal
.data/

# OS / editor cruft
.DS_Store
*.log

# brainstorm companion (mockups, transient state)
.superpowers/

# local worktrees (subagent-driven execution)
.worktrees/
.clone/
.claude/worktrees/
.claire/

# personal / runtime Claude Code state (shared skills under .claude/skills/ are tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# internal development artifacts — kept locally, not published
# (plans, specs, mockups, design explorations, audit findings)
docs/superpowers/
app/docs/
docs/archive/
docs/ui-demos/
docs/redesign/
docs/brand-preview/
docs/ux-audit/
docs/grid-*.md
docs/*.html
warehouse-redesign.html
docs/website-preview/
